import type { CollectorEnv } from './collector-env.ts';
import type { CollectorsRegistry } from './registry.ts';

export function registerAllCollectors(_registry: CollectorsRegistry, _env: CollectorEnv) {
  // Populated incrementally during Task 3 collector factoryization.
}

