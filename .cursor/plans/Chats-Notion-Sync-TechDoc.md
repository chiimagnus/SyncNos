# Chats 同步到 Notion 技术文档（SyncNos）

> **状态**：✅ V1 核心功能已实现
> **日期**：2025-12-31

本文档描述 SyncNos 中"微信聊天对话同步到 Notion"的完整技术方案，包括数据结构、同步流程、增量更新机制等。

## 1. 概述

### 1.1 功能目标

将 SyncNos 中的微信聊天对话（通过 OCR 解析的截图消息）同步到 Notion，使用户可以在 Notion 中归档、搜索和管理聊天记录。

### 1.2 设计原则

- **遵循现有架构**：使用 `NotionSyncEngine + Adapter` 模式，与其他数据源（Apple Books、GoodLinks、WeRead、Dedao）保持一致
- **增量更新**：基于 UUID + contentHash 实现智能增量同步，避免重复上传
- **本地记录优先**：优先使用本地 UUID → blockId 映射，减少 Notion API 调用

## 2. Notion 数据库结构

### 2.1 数据库位置

```
用户选择的 Notion 父页面
└── SyncNos-Chats (Database)
    ├── 联系人1 (Page)
    │   └── 消息列表 (Bulleted List Items)
    ├── 联系人2 (Page)
    │   └── 消息列表 (Bulleted List Items)
    └── ...
```

### 2.2 数据库属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| Name | Title | 对话名称（联系人名） |
| Contact | Rich Text | 联系人名称（冗余字段） |
| Message Count | Number | 消息数量 |
| Highlight Count | Number | 消息数量（兼容字段） |
| Last Sync Time | Date | 最后同步时间 |
| Asset ID | Rich Text | 对话唯一标识 (UUID) |

### 2.3 页面内容结构

每个对话页面内部结构：

```
📄 联系人名称
├── [系统属性区] Name, Contact, Message Count, Last Sync Time
└── [内容区]
    ├── 🔘 Heading: "Highlights" (可选)
    └── 🔘 消息列表 (Bulleted List Items)
        ├── • [uuid:xxx] style:blue | modified:abc123
        │     消息内容1
        │     └── Sender: Me
        ├── • [uuid:yyy] style:green | modified:def456
        │     消息内容2
        │     └── Sender: 联系人名
        └── • [uuid:zzz] style:gray | modified:ghi789
              系统消息内容
              └── Sender: System
```

## 3. 消息在 Notion 中的展示

### 3.1 消息块格式 (Numbered List Item)

每条消息作为一个 `numbered_list_item` 块，采用简洁的层级结构：

**父块**：发送者名称
- 仅包含 UUID（灰色背景，用于增量同步识别）
- 发送者名称（如 "Me"、"朋友"、"System"）

**子块**：消息内容
- 嵌套的 bulleted_list_item，显示实际消息文本

> **设计说明**：根据用户反馈，简化了消息格式，移除了 `modified time` 和 `style color`，使展示更加清晰简洁。

### 3.2 示例展示

在 Notion 中的实际效果：

```
1. [uuid:123e4567-e89b-12d3-a456-426614174000]
   Me
     • 你好，今天天气真好

2. [uuid:223e4567-e89b-12d3-a456-426614174001]
   朋友
     • 是啊，适合出去走走

3. [uuid:323e4567-e89b-12d3-a456-426614174002]
   System
     • 下午 3:00
```

### 3.3 视觉结构说明

| 层级 | 内容 | 格式 |
|------|------|------|
| 父块 | `[uuid:xxx]` + 发送者名称 | numbered_list_item |
| 子块 | 消息文本内容 | bulleted_list_item |

## 4. 同步流程

### 4.1 首次同步（全量同步）

```
┌─────────────────────────────────────────────────────────────┐
│                      首次同步流程                            │
├─────────────────────────────────────────────────────────────┤
│  1. 检查 Notion 配置（Token + Page ID）                      │
│  2. 确保 SyncNos-Chats 数据库存在                            │
│  3. 为对话创建新页面（设置标题、属性）                         │
│  4. 从本地缓存获取所有消息                                    │
│  5. 转换为 UnifiedHighlight 格式                             │
│  6. 构建 Notion blocks（带 UUID header）                     │
│  7. 批量追加到页面（底部）                                    │
│  8. 保存 UUID → blockId 映射到本地                           │
│  9. 更新 Last Sync Time                                     │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 增量同步

```
┌─────────────────────────────────────────────────────────────┐
│                      增量同步流程                            │
├─────────────────────────────────────────────────────────────┤
│  1. 检查 Notion 配置                                        │
│  2. 确保数据库和页面存在                                     │
│  3. 从本地缓存获取所有消息                                    │
│  4. 加载本地 UUID → blockId 映射                             │
│     ├── 有映射：使用本地记录（快速路径）                       │
│     └── 无映射：从 Notion 查询现有 blocks（回退路径）          │
│  5. 比较每条消息：                                           │
│     ├── UUID 存在 + contentHash 相同 → 跳过                  │
│     ├── UUID 存在 + contentHash 不同 → 更新块内容             │
│     └── UUID 不存在 → 追加新块（到页面底部）                   │
│  6. 保存新的 UUID → blockId 映射                             │
│  7. 更新 Last Sync Time                                     │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 新消息添加位置

**新消息追加到页面底部**（与聊天的阅读顺序一致）。

技术原因：
- Notion API 的 `append_block_children` 只支持追加到末尾
- 这与聊天记录的自然阅读顺序一致（旧消息在上，新消息在下）

### 4.4 消息更新

已存在的消息如果内容发生变化（如用户修改了发送者昵称），会通过 `PATCH /blocks/{block_id}` 原地更新，不改变位置。

## 5. 数据模型转换

### 5.1 ChatMessage → UnifiedHighlight

```swift
init(from message: ChatMessage, contactName: String) {
    self.uuid = message.id.uuidString
    
    // text 存储 sender name（将作为 Notion 中的父块）
    if let senderName = message.senderName, !senderName.isEmpty {
        self.text = senderName
    } else if message.isFromMe {
        self.text = "Me"
    } else {
        self.text = contactName
    }
    
    // note 存储消息内容（将作为 Notion 中的子块）
    self.note = message.content
    
    // Chats 不需要颜色索引、时间戳等字段
    self.colorIndex = nil
    self.dateAdded = nil
    self.dateModified = nil
    self.location = nil
    self.source = .chats
}
```

> **设计说明**：为实现 "Sender Name 作为主块，消息内容作为子块" 的格式，巧妙利用了通用同步逻辑的字段映射：
> - `text` → 父块内容（存储 sender name）
> - `note` → 子块内容（存储消息内容）
> 
> 这种设计**无需修改 NotionHelperMethods**，通用逻辑自动产生正确格式。其他字段（colorIndex、dateAdded、dateModified）设为 nil，因为 Chats 不需要这些信息。

### 5.2 ChatBookListItem → UnifiedSyncItem

```swift
init(from chat: ChatBookListItem) {
    self.itemId = chat.id
    self.title = chat.name
    self.author = ""
    self.url = nil
    self.source = .chats
    self.highlightCount = chat.messageCount
}
```

## 6. 增量更新机制

### 6.1 contentHash 计算

每条消息的 contentHash 基于以下内容计算 SHA-256 前 16 位：
- 发送者名称 (text)
- 消息内容 (note)
- 其他字段为空（Chats 不使用 style、dateAdded 等）

```swift
let payload = [text, note, style, added, modified, location].joined(separator: "\n")
let digest = SHA256.hash(data: payload.data(using: .utf8)!)
let contentHash = String(digest.prefix(16))  // 取前16位
```

### 6.2 本地记录存储

同步完成后，保存 UUID → blockId 映射到本地 SQLite：

| 字段 | 类型 | 说明 |
|------|------|------|
| uuid | String | 消息 UUID |
| notionBlockId | String | Notion block ID |
| contentHash | String | 内容哈希 |
| sourceKey | String | 数据源标识 ("chats") |
| bookId | String | 对话 ID |

### 6.3 增量判定逻辑

```
对于每条本地消息：
  1. 计算 localContentHash
  2. 查找 existingMap[uuid]
     ├── 找到 + remoteHash == localHash → 跳过（无变化）
     ├── 找到 + remoteHash != localHash → 加入更新列表
     └── 未找到 → 加入追加列表
```

## 7. API 调用模式

### 7.1 首次同步 API 调用

1. `POST /databases/{db_id}/query` - 检查页面是否存在
2. `POST /pages` - 创建新页面
3. `PATCH /pages/{page_id}` - 更新页面属性
4. `PATCH /blocks/{page_id}/children` - 批量追加消息块

### 7.2 增量同步 API 调用

1. `POST /databases/{db_id}/query` - 查找现有页面
2. `GET /blocks/{page_id}/children` - 获取现有块（仅回退路径）
3. `PATCH /blocks/{block_id}` - 更新已变化的块
4. `PATCH /blocks/{page_id}/children` - 追加新消息

### 7.3 速率限制

- 读取：8 RPS
- 写入：3 RPS
- 自动重试：429 和 409 错误时指数退避

## 8. 使用方式

### 8.1 单个对话同步

在 ChatDetailView 工具栏点击同步按钮：

```swift
// 内部调用
await chatViewModel.syncConversation(contact)
```

### 8.2 批量同步

在 ChatListView 右键菜单选择"Sync Selected to Notion"：

```swift
// 内部调用
chatViewModel.batchSync(
    contactIds: selectedIds,
    concurrency: NotionSyncConfig.batchConcurrency
)
```

### 8.3 通过 MainListView 快捷键

- `⌘⇧S` - 同步选中项到 Notion

## 9. 同步状态追踪

### 9.1 UI 状态显示

- **ChatListView**：联系人行显示黄色旋转图标表示正在同步
- **ChatDetailView**：工具栏按钮显示 ProgressView 和进度提示

### 9.2 ViewModel 状态

```swift
@Published var syncingContactIds: Set<String>  // 正在同步的对话ID
@Published var syncProgress: [String: String]  // 进度文本映射
```

## 10. 错误处理

| 错误类型 | 处理方式 |
|----------|----------|
| Notion 配置缺失 | 弹窗提示前往设置 |
| 网络错误 | 显示错误信息，可重试 |
| API 限流 (429) | 自动指数退避重试 |
| 认证失败 (401) | 弹窗提示重新授权 |

## 11. 未来扩展

### 11.1 潜在增强

- [ ] 支持 PerBook 模式（每个对话独立数据库）
- [ ] 支持图片消息同步（需 Notion 文件 API）
- [ ] 支持自动同步（Smart Sync）
- [ ] 支持双向同步（从 Notion 读取变更）

### 11.2 Smart Sync 集成

未来可添加 `ChatsAutoSyncProvider` 实现自动增量同步：

```swift
protocol AutoSyncSourceProvider {
    var sourceKey: String { get }
    var isEnabled: Bool { get }
    func detectChanges() async throws -> Bool
    func performSync() async throws
}
```

## 12. 相关文件

- `SyncNos/Services/DataSources-To/Notion/SyncEngine/Adapters/ChatsNotionAdapter.swift`
- `SyncNos/ViewModels/Chats/ChatViewModel.swift`
- `SyncNos/Models/Core/UnifiedHighlight.swift`
- `SyncNos/Models/Core/HighlightColorScheme.swift`
- `SyncNos/Views/Chats/ChatDetailView.swift`
- `SyncNos/Views/Chats/ChatListView.swift`
